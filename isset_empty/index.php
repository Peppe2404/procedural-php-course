<?php

    //isset() = returns TRUE if a variable is declared and not null
    // empty() = returns TRUE if a variable is not declared, false, null, ""


    
    //$username = "BroCode";
    //echo isset($username); //restituisce 1 (true) poichè la variabile $username è dichiarata e non nulla 
    
    /*$username = true;

    if(isset($username)){ //se $username è dichiarato o non nullo o true
        echo "this variable is set"; //scrivi questo
    }
    else{
        echo "variable NOT set"; //altrimenti scrivi quest altro (false, non dichiarata, null)
    }*/



   /*$username = false;

    if(empty($username)){ //se $username non è dichiarato o è null o false
        echo "this variable is empty"; //scrivi questo
    }
    else{
        echo "variable NOT empty"; //altrimenti scrivi quest altro (se true, dichiarata, not null)
    }*/



?>
